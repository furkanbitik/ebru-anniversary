{
  "name": "ebru-anniversary",
  "private": true,
  "version": "1.0.0",
  "description": "Ebru & Furkan — anı sitesi (statik, Cloudflare Workers)",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
